#!/usr/bin/env bash
# Idea 61 / 63 — NO mueve ventanas automáticamente (H/M según compositor).
# Plantilla para que documentes reglas en GNOME/KDE o scripts propios.
# Ejemplo GNOME: gsettings para atajos; en KDE: reglas de ventana en Configuración del sistema.
echo "Jarvis: colocación avanzada de ventanas va fuera del script principal; use reglas del compositor."
exit 0
