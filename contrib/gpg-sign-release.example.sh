#!/usr/bin/env bash
# Idea 51 — ejemplo de firma GPG para artefactos de release (ajuste IDs y rutas).
set -euo pipefail
# export GNUPGHOME=~/.gnupg
# ARTIFACT="./dist/mi_paquete.tar.gz"
# gpg --detach-sign --armor "$ARTIFACT"
# gpg --verify "${ARTIFACT}.asc" "$ARTIFACT"
echo "Edite este script con su keyid y rutas reales antes de usarlo en producción."
exit 0
